# Laravel & Editor
*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml

# IDEs & editor folders
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed

# Composer & NPM
/auth.json
/node_modules
npm-debug.log*
yarn-error.log*

# Build artifacts
/public/build
/public/hot

# Storage keys and private files
/storage/*.key
/storage/pail

# Don’t ignore our public storage symlink
# (needed so public/storage → storage/app/public is included)
/public/storage

# Vendor libraries
/vendor

# ─── Storage ────────────────────────────────────────────────
# Ignore everything under storage/app...
/storage/app/*
# …except storage/app/public (we want these in Git)
!/storage/app/public

# Thumbnails, caches, OS files
Thumbs.db
